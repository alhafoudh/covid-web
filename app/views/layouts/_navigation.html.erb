<nav class="bg-white" data-controller="toggle" id="menu">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <span class="h-8 w-8">🏥</span>
          <span class="ml-2 font-bold text-black"><%= t(:app_title) %></span>
        </div>
        <div class="hidden md:block">
          <div class="ml-10 flex items-baseline space-x-4">
            <%= active_link_to t(:'menu.testing'), root_path, active: :exclusive, class_active: 'link link-active', class_inactive: 'link' %>
            <%= active_link_to t(:'menu.vaccination'), vaccination_root_path, class_active: 'link link-active', class_inactive: 'link' %>
          </div>
        </div>
      </div>
      <div class="md:hidden">
        <!-- Mobile menu button -->
        <button class="bg-gray-800 inline-flex items-center justify-center p-2 rounded-md text-gray-400 focus:text-white focus:outline-none" data-action="toggle#toggle" data-toggle-target="menu-list,icon-burger,icon-check">
          <span class="sr-only"><%= t(:open_main_menu) %></span>

          <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true" data-toggle-name="icon-burger">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>

          <svg class="hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true" data-toggle-name="icon-check">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
      </div>
    </div>
    <div data-toggle-name="menu-list" class="hidden md:hidden flex flex-col">
      <%= active_link_to t(:'menu.testing'), root_path, active: :exclusive, class_active: 'link link-active', class_inactive: 'link' %>
      <%= active_link_to t(:'menu.vaccination'), vaccination_root_path, class_active: 'link link-active', class_inactive: 'link' %>
    </div>
  </div>
</nav>
