<div class="flex lg:flex-row flex-col mb-1 lg:h-8">
  <div class="flex flex-col lg:mb-2 lg:max-w-sm w-full">
    <span class="text-xl font-bold"><%= title %></span>
  </div>
  <div class="flex items-stretch w-full">
    <% test_dates.map do |test_date| %>
      <div class="w-full flex items-center justify-center text-center text-sm">
        <%= l test_date.date, format: '%a %d %b' %>
      </div>
    <% end %>
  </div>
</div>